# Source Layout

本目录预留后续 Python 实现。

当前只包含占位包结构：

- `novel2script/schema/`：未来放置 Schema 加载、版本管理和结构辅助函数。
- `novel2script/validators/`：未来放置确定性校验器。
- `novel2script/exporters/`：未来放置 Fountain 等导出器。
- `novel2script/agents/`：未来放置 Agent 编排接口和 Prompt 调用边界。

第一阶段不实现完整业务逻辑。
